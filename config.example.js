module.exports = {
    jsonFix: "window.YTD.tweet.part0 = ",   // Text to be removed to parse tweet.js as JSON correctly

    consumerKey: "",                        // Twitter application consumer key
    consumerSecret: "",                     // Twitter application consumer secret
    accessToken: "",                        // Twitter application access token
    accessTokenSecret: ""                   // Twitter application access token secret
}